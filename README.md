# To-Do List App

A simple To-Do List application built with **HTML, CSS, and JavaScript**.  
It allows users to add, remove, and persist tasks using **localStorage**.

## Features
- Add new tasks
- Remove tasks
- Tasks saved in localStorage (persist after refresh)
- Clean and responsive UI

## How to Run
1. Clone the repository:
   ```bash
   git clone https://github.com/Vishal-git123/To-do-list.git
